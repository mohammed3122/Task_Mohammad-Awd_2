void main() {
  List<num> numbers = List.filled(5, 0);

  numbers[0] = 2;
  numbers[1] = 4;
  numbers[2] = 6;

  numbers.add(8);
  numbers.add(10);

  print(numbers);

  // if I add elements to fixed List that ..
  //cause excption.
}
